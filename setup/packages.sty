\ProvidesPackage{setup/packages}

\usepackage[T1]{fontenc}
\usepackage[T1]{url}  
\usepackage[utf8]{inputenc}
\usepackage{float}

% font
\usepackage{lmodern}

\usepackage[ngerman]{babel}
\usepackage[ngerman]{translator}

% load order important
\usepackage{hyperref}

\usepackage{graphicx}   
\usepackage{wrapfig}
\usepackage{pdfpages}
\usepackage[outputdir=build]{minted}
\usepackage[sanitizesort, acronym]{glossaries}
\usepackage{pgfplots}
\pgfplotsset{compat=1.18}
\usepackage{longtable}
\usepackage{multicol}
\usepackage{pgfgantt}

\usepackage{amsmath} 
\usepackage{siunitx}

\usepackage{microtype}

% Footnote Formatting
\usepackage{scrlayer-scrpage}
\usepackage{tablefootnote}
\interfootnotelinepenalty 9999

\usepackage{scrhack}

\usepackage[babel,german=quotes]{csquotes}
\usepackage[backend=biber, language=ngerman, 
style=numeric, 
giveninits=true,	% Vornamen abkürzen *** geht wenn Norm in Author steht und ~ 2017-07-31 
maxbibnames=2,
maxcitenames=1, 	% Kürzel nur aus 1. Autor
backref=true, 		% Rückverweise auf Zitatseiten
maxnames=50,
block=none]
{biblatex}

\ExecuteBibliographyOptions{%
	bibencoding=utf8, % wenn .bib in utf8
	bibwarn=true,     % Warnung bei fehlerhafter bib-Datei
}
\DefineBibliographyStrings{ngerman}{andothers={et\ al\adddot}} % "u.a." zu "et al." 
\DefineBibliographyStrings{ngerman}{and={;}} % "und" zu ";"
\DefineBibliographyStrings{ngerman}{urlseen={online - zuletzt aufgerufen am}}
\DefineBibliographyStrings{ngerman}{bibliography={Literaturverzeichnis}}
\setcounter{biburllcpenalty}{7000}
\setcounter{biburlucpenalty}{8000}

\usepackage[normalem]{ulem}   % Unterstreichen und durchstreichen  Probleme mit bibtex

\DeclareNameAlias{default}{family-given}  % 2019-09-23 


\usepackage{tikz}
\usetikzlibrary{matrix} %für Morphologischenkasten
\usetikzlibrary{backgrounds} 		% Verbindugslinie im Hintergrund
\usetikzlibrary{angles,quotes}		%Für Winkel
\usetikzlibrary{babel}					% Probleme mit "" behoben
\usetikzlibrary{decorations.pathreplacing}
\newcommand\savepos[1]{\tikz[remember picture]{\node(#1){};}}
\tikzstyle{startstop} = [rectangle, rounded corners, minimum width=3cm, minimum height=1cm,text centered, draw=black, fill=red!30]
\tikzstyle{io} = [trapezium, trapezium left angle=70, trapezium right angle=110, minimum width=3cm, minimum height=1cm, text centered, draw=black, fill=blue!30]
\tikzstyle{process} = [rectangle, minimum width=3cm, minimum height=1cm, text centered, draw=black, fill=orange!30]
\tikzstyle{decision} = [diamond, minimum width=3cm, minimum height=1cm, text centered, draw=black, fill=green!30]
\tikzstyle{arrow} = [thick,<->,>=stealth]
\tikzstyle{sig} = [thick,->,>=stealth]
\tikzstyle{BBB} = [rectangle, rounded corners, minimum width=2cm, minimum height=1cm,text centered, draw=black]
\tikzstyle{Dyn} = [rectangle, minimum width=2cm, minimum height=1cm, text centered, draw=black]
\usetikzlibrary{shapes,arrows}